n;let teams=G.cup.teams,next=[];for(let i=0;i<teams.length;i+=2){let a=teams[i],b=teams[i+1];if(!b){next.push(a);continue}let da=findDiv(a),db=findDiv(b),sa=strength(a,da),sb=strength(b,db),wa=Math.random()<(sa/(sa+sb));let w=wa?a:b;next.push(w);G.cup.log.unshift(`${a} x ${b} → ${w} classificado`)}G.cup.teams=next;G.cup.round++;if(next.length===1){G.cup.winner=next[0];let h=G.history.find(x=>x.year===G.year);if(h)h.cup=next[0];addNews('Campeão da Copinha',next[0]+' conquistou a Copinha Gaúcha.');let superWinner=Math.random()<.5?next[0]:G.cup.gauchoChamp;G.cup.superWinner=superWinner;addNews('Supercopa Gaúcha',`${G.cup.gauchoChamp} x ${next[0]} — campeão: ${superWinner}.`) }saveGame(false);renderCup()}
function newSeason(){if(!G.seasonEnded||!G.cup||!G.cup.winner)return toast('Termina a Copinha antes.');let rank=standing(G.division).findIndex(x=>x.t===G.club);if(G.division==='B'&&rank<2)G.division='A2';else if(G.division==='A2'&&rank<2)G.division='A1';for(const d in DIV){G.tables[d]={};DIV[d].clubs.forEach(c=>G.tables[d][c]={J:0,V:0,E:0,D:0,GP:0,GC:0,SG:0,P:0});G.schedules[d]=roundRobin(DIV[d].clubs)}G.year++;G.round=0;G.phase='league';G.playoffs=null;G.seasonEnded=false;G.cup=null;money(-15000,'Custos de pré-temporada');addNews('Nova temporada',`${G.year} começou. Divisão atual: ${DIV[G.division].name}.`);renderAll()}
function findDiv(c){for(const d in DIV)if(DIV[d].clubs.includes(c))return d;return 'B'}
const physios=[{name:'Eduardo Casalinho',stars:5,pct:90,sal:7500,fee:9000},{name:'Vicente Almeida',stars:4,pct:77,sal:5000,fee:6000},{name:'Felipe Nunes',stars:3,pct:70,sal:3200,fee:3500}];const preps=[{name:'Rafael Tavares',stars:5,pct:88,sal:7000,fee:8000},{name:'Marcos Vidal',stars:4,pct:80,sal:4800,fee:5000},{name:'André Pires',stars:3,pct:72,sal:3000,fee:3000}];const sponsors=[{name:'Mercado Avenida',slot:'Master',monthly:10000,bonus:12000,months:12},{name:'Clínica Movimento',slot:'Master',monthly:7600,bonus:8000,months:12},{name:'AutoSul',slot:'Master',monthly