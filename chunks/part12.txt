lue||0;$('bonusStatus').textContent=G.bonus?fmt(G.bonus)+' por vitória':'Sem bicho';addNews('Bicho definido',G.bonus?`Diretoria prometeu ${fmt(G.bonus)} por vitória.`:'Partida sem bicho.');saveGame(false)}
function disciplineEvent(min){ensureTactics();let lineup=G.lineup.map(n=>G.roster.find(p=>p[0]===n)).filter(Boolean);if(!lineup.length)return '';let p=lineup[Math.floor(Math.random()*lineup.length)],k=pkey(p[0]);if(Math.random()<.18){if(Math.random()<.14){G.susp[k]=1;return `${min}' 🟥 ${p[0]} expulso! Suspenso por 1 jogo.`}G.cards[k]=(G.cards[k]||0)+1;if(G.cards[k]>=3){G.cards[k]=0;G.susp[k]=1;return `${min}' 🟨 ${p[0]} recebeu o 3º amarelo e está suspenso por 1 jogo.`}return `${min}' 🟨 Amarelo para ${p[0]} (${G.cards[k]}/3).`}return ''}
function serveSuspensions(){for(const k in G.susp)if(G.susp[k]>0)G.susp[k]--;ensureTactics();G.lineup=G.lineup.filter(n=>!(G.susp[pkey(n)]>0));while(G.lineup.length<11){let p=G.roster.find(x=>!G.lineup.includes(x[0])&&!(G.susp[pkey(x[0])]>0));if(!p)break;G.lineup.push(p[0])}}
function playRound(live=true){if(G.phase==='cup'||G.seasonEnded){go('cup');return toast('Os estaduais acabaram. Agora é Copinha Gaúcha.')}let g=userGame();let any=['A1','A2','B'].some(d=>currentGames(d).some(x=>!x.played));if(!any){if(G.phase==='league'&&G.round>=maxRegularRounds())return startStatePlayoffs();return toast('Não há jogos pendentes nesta data.')}if(g&&G.susp){ensureTactics();let suspended=G.lineup.filter(n=>G.susp[pkey(n)]>0);if(suspended.length)return toast('Tem jogador suspenso na escalação. Ajusta antes de jogar.')}$('playRound').disabled=true;$('instantRound').disabled=true;$('matchLog').innerHTML='';let m=0;let interval=setInterval(()=>{m+=live?3:15;$('clock').textContent=Math.min(m,90)+"'";for(const d of ['A1','A2','B'])for(const x of currentGames(d)){if(x.played)continue;simulateGoal(d,x)}if(g)$('matchScore').textContent=g.hg+' x '+g.ag;if(live&&g){let de=disciplineEvent(m);if(de)$('matchLog').innerHTML=`<div class=item>${de}</div>`+$('mat