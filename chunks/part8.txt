 renderSponsors(){$('currentSponsor').innerHTML=G.sponsor?`Atual: <b>${G.sponsor.name}</b> • ${G.sponsor.slot} • ${fmt(G.sponsor.monthly)}/mês <button class=btn onclick="cancelSponsor()">Rescindir</button>`:'Sem patrocinador master.';$('sponsorOptions').innerHTML=sponsors.map((s,i)=>`<div class=item><b>${s.name}</b> • ${s.slot} • ${fmt(s.monthly)}/mês • bônus ${fmt(s.bonus)} • ${s.months} meses <button class=btn onclick="acceptSponsor(${i})">${G.sponsor?.name===s.name?'Renovar':'Assinar'}</button></div>`).join('')}
function renderMarket(){if(!G.juniors)G.juniors=makeJuniors();let q=$('marketQ').value.toLowerCase();$('marketList').innerHTML=G.db.filter(p=>(p.n+' '+p.c).toLowerCase().includes(q)).slice(0,160).map(p=>`<div class=item><b>${p.n}</b> • ${p.p} • ${p.c} <span class=clubtag>${DIV[p.d].name}</span> • força <b>${p.ov}</b> • valor-base ${fmt(p.value)} <button class=btn onclick="buyPlayer('${p.id}')">Negociar</button></div>`).join('');$('juniorList').innerHTML=G.juniors.slice(0,60).map(p=>`<div class=item><b>${p.n}</b> <span class="statusPill junior">PROMESSA</span> • ${p.age} anos • ${p.p} • força ${p.ov} • potencial ${p.potential} • ${fmt(p.value)} <button class=btn onclick="buyJunior('${p.id}')">Comprar</button> <button class=btn onclick="loanJunior('${p.id}')">Empréstimo</button></div>`).join('')}
function pkey(n){return n.replace(/[^a-z0-9]/gi,'_')}function ensureTactics(){G.cards=G.cards||{};G.susp=G.susp||{};G.roles=G.roles||{captain:'Gustavo Gavila',penalty:'Raphael Costa',freeKick:'Robinho',corner:'Flávio'};if(!G.lineup||G.lineup.length!==11)G.lineup=G.roster.filter(p=>!(G.susp[pkey(p[0])]>0)).slice(0,11).map(p=>p[0])}
function formationRows(){let f=$('formation')?.value||'4-4-2';return f==='4-3-3'?[1,4,3,3]:f==='4-2-3-1'?[1,4,2,3,1]:f==='3-5-2'?[1,3,5,2]:f==='4-1-4-1'?[1,4,1,4,1]:[1,4,4,2]}
function renderLineup(){ensureTactics();let avail=G.roster.filter(p=>!(G.susp[pkey(p[0])]>0));let rows=formationRows(),idx=0,html='<div class=formationGrid>';for(le