r.flatMap(x=>[x.h,x.a])));if(G.club&&!schedNames.has(G.club)&&schedNames.has('Guri FC'))renameUserClub('Guri FC',G.club);$('splash').classList.add('hide');$('app').classList.remove('hide');renderAll();toast('Save carregado.')}catch(e){toast('Save inválido.')}}function exportSave(){let blob=new Blob([JSON.stringify(G,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`futchebol-guri-${G.year}.json`;a.click();URL.revokeObjectURL(a.href)}
$('loadBtn').onclick=loadGame;$('newBtn').onclick=()=>{localStorage.removeItem('futchebol-guri-save-v3');G=freshState();buildEditor();toast('Novo jogo preparado.');};function buildEditor(){$('editor').innerHTML=G.roster.map((p,i)=>`<div class=row item><input value="${p[0]}" data-i="${i}" style="flex:1"><select data-p="${i}">${['GOL','ZAG','LD','LE','VOL','MC','MEI','PD','PE','ATA'].map(x=>`<option ${x===p[1]?'selected':''}>${x}</option>`).join('')}</select></div>`).join('')}$('enter').onclick=()=>{$('editor').querySelectorAll('input').forEach(e=>G.roster[e.dataset.i][0]=e.value.trim()||('Jogador '+(+e.dataset.i+1)));$('editor').querySelectorAll('select').forEach(e=>G.roster[e.dataset.p][1]=e.value);let oldClub=G.club,newClub=$('clubName').value.trim()||'Guri FC';renameUserClub(oldClub,newClub);G.club=newClub;$('splash').classList.add('hide');$('app').classList.remove('hide');addNews('Professor apresentado',`Tu assumiu o ${G.club}. A diretoria quer evolução temporada após temporada.`);renderAll();saveGame(false)};buildEditor();renderAll();
</script></body></html>