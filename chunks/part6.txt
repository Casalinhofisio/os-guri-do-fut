ência — '+p.n);G.roster.push([p.n,p.p,p.ov]);G.db=G.db.filter(x=>x.id!==id);addNews('Reforço contratado',`${p.n}, ${p.p}, chegou do ${p.c} por ${fmt(asking)}.`);renderSquad();renderMarket();saveGame(false)}
function buyJunior(id){let p=G.juniors.find(x=>x.id===id);if(!p)return;if(G.cash<p.value)return toast('Caixa insuficiente.');if(!confirm(`Contratar ${p.n}, ${p.age} anos, por ${fmt(p.value)}?`))return;money(-p.value,'Contratação de promessa — '+p.n);G.roster.push([p.n,p.p,p.ov]);G.juniors=G.juniors.filter(x=>x.id!==id);addNews('Joia contratada',`${p.n}, ${p.age} anos, chegou por ${fmt(p.value)}. Potencial estimado ${p.potential}.`);renderSquad();renderMarket();saveGame(false)}
function loanJunior(id){let p=G.juniors.find(x=>x.id===id);if(!p)return;let fee=Math.round(p.value*.12);if(G.cash<fee)return toast('Caixa insuficiente.');money(-fee,'Taxa de empréstimo — '+p.n);G.roster.push([p.n,p.p,p.ov]);G.juniors=G.juniors.filter(x=>x.id!==id);addNews('Empréstimo confirmado',`${p.n}, ${p.age} anos, chega por empréstimo até o fim da temporada.`);renderSquad();renderMarket();saveGame(false)}
function randomMarket(){if(Math.random()>.65)return;let p=G.db[Math.floor(Math.random()*G.db.length)];if(!p)return;let same=G.db.filter(x=>x.d===p.d&&x.c!==p.c);let dest=same[Math.floor(Math.random()*same.length)]?.c;if(!dest)return;addNews('Mercado da liga',`${p.n} entrou no radar do ${dest}.`)}
function renderTables(){let d=$('divisionSelect').value;$('tableTitle').textContent=DIV[d].name+' • '+G.year;$('tableBody').innerHTML=standing(d).map((x,i)=>`<tr><td>${i+1}</td><td><b>${x.t}</b></td><td>${x.J}</td><td>${x.V}</td><td>${x.E}</td><td>${x.D}</td><td>${x.GP}</td><td>${x.GC}</td><td>${x.SG}</td><td><b>${x.P}</b></td></tr>`).join('')}
function renderNews(){let rows=G.news.map(n=>`<div class=item><div class=newsTitle>${n.t}</div><div class=mut>${n.y} • ${n.b}</div></div>`).join('');$('inboxList').innerHTML=rows;$('pressList').innerHTML=rows;$('homeNews').innerHTML=G.news.slice(0,5).m