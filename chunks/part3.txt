'Lima','Martins','Medeiros','Moraes','Nunes','Oliveira','Pereira','Pires','Prado','Ramos','Ribeiro','Rocha','Rodrigues','Santos','Silva','Souza','Tavares','Teixeira','Vieira'];const pos=['GOL','GOL','ZAG','ZAG','ZAG','LD','LE','VOL','VOL','MC','MC','MEI','PD','PE','ATA','ATA','ATA','MEI'];
function seeded(a,b){let x=(a*9301+b*49297+233280)%233280;return x/233280}function makeDb(){let out=[],ci=0;for(const d of ['B','A2','A1'])for(const c of DIV[d].clubs){if(c==='Guri FC'){ci++;continue}let [lo,hi]=DIV[d].range;for(let i=0;i<22;i++){let ov=Math.round(lo+seeded(ci+1,i+4)*(hi-lo));if(c==='Internacional'||c==='Grêmio')ov=Math.max(74,Math.min(91,ov+8));if(c==='Juventude'||c==='Caxias')ov=Math.max(66,Math.min(84,ov+5));let n=names[(ci*7+i*3)%names.length]+' '+surn[(ci*5+i*7)%surn.length];out.push({id:d+'-'+ci+'-'+i,n,c,p:pos[i%pos.length],ov,d,value:Math.round(ov*ov*(d==='A1'?900:d==='A2'?260:70))})}ci++}return out}
function makeJuniors(){let out=[];for(let i=0;i<36;i++){let age=16+(i%5),ov=14+Math.floor(seeded(i+71,i+9)*30),potential=Math.min(88,ov+18+Math.floor(seeded(i+91,i+5)*26)),n=names[(i*5+7)%names.length]+' '+surn[(i*9+3)%surn.length];out.push({id:'J-'+i,n,p:pos[(i+3)%pos.length],age,ov,potential,value:7000+ov*900,club:['Livre','Base do Interior','Projeto Jovem','Academia Sul'][i%4]})}return out}
function roundRobin(arr){let a=[...arr];if(a.length%2)a.push('FOLGA');let rounds=[];for(let r=0;r<a.length-1;r++){let games=[];for(let i=0;i<a.length/2;i++){let h=a[i],v=a[a.length-1-i];if(h!=='FOLGA'&&v!=='FOLGA')games.push({h:r%2?h:v,a:r%2?v:h,hg:0,ag:0,played:false})}rounds.push(games);a=[a[0],a[a.length-1],...a.slice(1,-1)]}return rounds}
function freshState(){let tables={},schedules={};for(const d in DIV){tables[d]={};DIV[d].clubs.forEach(c=>tables[d][c]={J:0,V:0,E:0,D:0,GP:0,GC:0,SG:0,P:0});schedules[d]=roundRobin(DIV[d].clubs)}return{year:2026,division:'B',round:0,club:'Guri FC',cash:220000,confidence:68,roster:structuredClone(starters),db:makeDb(),juniors:makeJuniors(),cards:{},susp:{},lineup:[],roles:{captain:'Gustavo Gavila',penalty:'Raphael Costa',freeKick:'Robinho',corner:'Flávio'},bonus:0,tables,schedules,news:[{y:2026,t:'Carreira iniciada',b:'Guri FC estreia na Série B Gaúcha.'}],ledger:[{y:2026,t:'Saldo inicial',v:220000}],phys:null,prep:null,sponsor:null,history:[],cup:null,playoffs:null,phase:'league',seasonEnded:false}}
let G=freshState();
function toast(t){$('toast').textContent=t;$('toast').style.display='block';setTimeout(()=>$('toast').style.display='none',2200)}function addNews(t,b){G.news.unshift({y:G.year,t,b});G.news=G.news.slice(0,80)}function money(v,desc){G.cash+=v;G.ledger.unshift({y:G.year,t:desc,v});if(v<0)addNews('Despesa registrada',desc+' • '+fmt(v));else addNews('Receita registrada',desc+' • +'+fmt(v));renderFinance()}
function strength(team,d){if(team===G.club)return Math.round(G.roster.reduce((a,x)=>a+x[2],0)/G.roster.length);let ps=G.db.filter(x=>x.c===team);return ps.length?Math.round(ps.reduce((a,x)=>a+x.ov,0)/ps.length):DIV[d].range[0]}
function apply(d,g){if(g.applied)return;g.applied=true;let A=G.tables[d][g.h],B=G.tables[d][g.a];for(const x of [A,B])x.J++;A.GP+=g.hg;A.GC+=g.ag;B.GP+=g.ag;B.GC+=g.hg;A.SG=A.GP-A.GC;B.SG=B.GP-B.GC;if(g.hg>g.ag){A.V++;B.D++;A.P+=3}else if(g.hg<g.ag){B.V++;A.D++;B.P+=3}else{A.E++;B.E++;A.P++;B.P++}}
function standing(d){return Object.entries(G.tables[d]).map(([t,s])=>({t,...s})).sort((a,b)=>b.P-a.P||b.V-a.V||b.SG-a.SG||b.GP-a.GP)}
function maxRegularRounds(){return Math.max(...Object.values(G.schedules).map(x=>x.length))}
function userGame(){if(G.phase==='playoffs'&&G.playoffs){let po=G.playoffs[G.division];return po?.games?.find(x=>x.h===G.club||x.a===G.club)||null}return (G.schedules[G.division][G.round]||[]).find(x=>x.h===G.club||x.a===G.club)||null}
function currentGames(d){if(G.phase==='playoffs')return G.playoffs?.[d]?.games||[];return G.schedules[d][G.round]||[]}
function clubColor(n){let h=0;for(let i=0;i<n.length;i++)h=(h*31+n.charCodeAt(i))%360;return `hsl(${h} 72% 48%)`}
function renameUserClub(oldName,newName){if(oldName===newName)return;for(const d in G.schedules)for(const rd of G.schedules[d])for(const g of rd){if(g.h===oldName)g.h=newName;if(g.a===oldName)g.a=newName}if(G.tables.B?.[oldName]){G.tables.B[newName]=G.tables.B[oldName];delete G.tables.B[oldName]}DIV.B.clubs=DIV.B.clubs.map(c=>c===oldName?newName:c)}
function simulateGoal(d,g){let hs=strength(g.h,d),as=strength(g.a,d),hp=.025+Math.max(-.012,Math.min(.025,(hs-as)/900)),ap=.025+Math.max(-.012,Math.min(.025,(as-hs)/900));if(Math.random()<hp)g.hg++;if(Math.random()<ap)g.ag++}
function completeOtherGames(){for(const d in DIV){let games=currentGames(d);for(const g of games){if(g.played)continue;for(let m=0;m<30;m++)simulateGoal(d,g);g.played=true;if(G.phase==='league')apply(d,g)}}}
function monthlyTick(){let wages=0;if(G.phys)wages+=G.phys.sal;if(G.prep)wages+=G.prep.sal;if(wages)money(-wages,'Folha da comissão técnica');if(G.sponsor)money(G.sponsor.monthly,'Patrocínio '+G.sponsor.name+' — mensalidade')}
function endRound(){completeOtherGames();if((G.round+1)%4===0)monthlyTick();randomMarket();G.round++;if(G.round>=G.schedules[G.division].length){finishSeason()}saveGame(false);renderAll()}
function koGame(h,a){return{h,a,hg:0,ag:0,played:false,pen:null}}
function startStatePlayoffs(){if(G.phase==='playoffs')return;G.phase='playoffs';G.playoffs={};for(const d of ['A1','A2']){let t=standing(d).slice(0,8).map(x=>x.t);G.playoffs[d]={stage:'Quartas de final',seeds:t,games:[koGame(t[0],t[7]),koGame(t[3],t[4]),koGame(t[1],t[6]),koGame(t[2],t[5])],winner:null}}let b=standing('B').slice(0,6).map(x=>x.t);G.playoffs.B={stage:'Quartas de final',seeds:b,games:[koGame(b[2],b[5]),koGame(b[3],b[4])],winner:null};addNews('Fases finais começaram','Gauchão, Divisão de Acesso e Série B entram no mata-mata ao mesmo tempo.');G.round=0;renderAll();saveGame(false)}
function koWinner(g,d){if(g.hg===g.ag){let hs=strength(g.h,d),as=strength(g.a,d);let w=Math.random()<(hs/(hs+as));g.pen=w?g.h:g.a;return g.pen}return g.hg>g.ag?g.h:g.a}
function advancePlayoffs(){for(const d of ['A1','A2','B']){let po=G.playoffs[d];if(!po||po.winner)continue;let wins=po.games.map(g=>koWinner(g,d));if(po.stage==='Quartas de final'){if(d==='B'){let s=po.seeds;po.stage='Semifinal';po.games=[koGame(s[0],wins[1]),koGame(s[1],wins[0])]}else{po.stage='Semifinal';po.games=[koGame(wins[0],wins[1]),koGame(wins[2],wins[3])]}}else if(po.stage==='Semifinal'){po.stage='Final';po.games=[koGame(wins[0],wins[1])]}else if(po.stage==='Final'){po.winner=wins[0];po.stage='Encerrado';po.games=[];addNews('Campeão estadual',`${po.winner} conquistou ${DIV[d].name}.`)}}
if(['A1','A2','B'].every(d=>G.playoffs[d]?.winner))finishSeason()}
function finishSeason(){if(G.seasonEnded)return;G.seasonEnded=true;G.phase='cup';let champs={A1:G.playoffs.A1.winner,A2:G.playoffs.A2.winner,B:G.playoffs.B.winner};G.history.unshift({year:G.year,A1:champs.A1,A2:champs.A2,B:champs.B,cup:'Pendente'});addNews('Estaduais encerrados',`Campeões: Gauchão ${champs.A1}; Acesso ${champs.A2}; Série B ${champs.B}.`);startCup(champs.A1)}
function startCup(gauchoChamp){let all=[...DIV.A1.clubs,...DIV.A2.clubs,...DIV.B.clubs];for(let i=all.length-1;i>0;i--){let j=Math.floor(Math.random()*(i+1));[all[i],all[j]]=[all[j],all[i]]}G.cup={gauchoChamp,teams:all.slice(0,32),round:0,winner:null,log:[]};addNews('Copinha Gaúcha começou','32 clubes das três divisões entram no mata-mata.')}function playCup(){if(!G.cup||G.cup.winner)retur