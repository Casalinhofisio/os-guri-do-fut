t r of rows){html+='<div class=formationRow>';for(let j=0;j<r;j++){let selected=G.lineup[idx]||avail[idx]?.[0]||'';html+=`<div class=slot><b>Posição ${idx+1}</b><select class=lineupSel data-i=${idx}>${avail.map(p=>`<option ${p[0]===selected?'selected':''} value="${p[0]}">${p[1]} • ${p[0]} (${p[2]})</option>`).join('')}</select></div>`;idx++}html+='</div>'}html+='</div>';$('pitch').innerHTML=html;let bench=G.roster.filter(p=>!G.lineup.includes(p[0]));$('bench').innerHTML=bench.map(p=>`<div class=item><b>${p[0]}</b> • ${p[1]} • ${p[2]} ${G.susp[pkey(p[0])]>0?'<span class="statusPill susp">SUSPENSO</span>':''}</div>`).join('');let roleNames=[['captain','Capitão'],['penalty','Pênaltis'],['freeKick','Faltas'],['corner','Escanteios']];$('roles').innerHTML=roleNames.map(([k,l])=>`<div class=rolebox><b>${l}</b><select onchange="G.roles['${k}']=this.value;saveGame(false)">${G.roster.map(p=>`<option ${G.roles[k]===p[0]?'selected':''}>${p[0]}</option>`).join('')}</select></div>`).join('');$('discipline').innerHTML=G.roster.map(p=>{let k=pkey(p[0]),y=G.cards[k]||0,su=G.susp[k]||0;return `<div class=item><b>${p[0]}</b> • amarelos: <span class="statusPill yellow">${y}/3</span> ${su?'<span class="statusPill susp">SUSPENSO 1 JOGO</span>':''}</div>`}).join('')}
function saveLineup(){let sels=[...document.querySelectorAll('.lineupSel')],vals=sels.map(x=>x.value);if(new Set(vals).size<vals.length)return toast('Não pode escalar o mesmo jogador duas vezes.');G.lineup=vals;addNews('Escalação definida','Professor confirmou os 11 titulares e as bolas paradas.');saveGame(false);renderLineup();toast('Escalação salva.')}function renderSquad(){ensureTactics();$('squadList').innerHTML=G.roster.map((p,i)=>{let k=pkey(p[0]),y=G.cards[k]||0,su=G.susp[k]||0;return `<div class=item><b>${p[0]}</b> • ${p[1]} • força <b>${p[2]}</b>${G.roles.captain===p[0]?' <span class=badge>CAPITÃO</span>':''} ${y?'<span class="statusPill yellow">🟨 '+y+'/3</span>':''} ${su?'<span class="statusPill susp">SUSPENSO</span