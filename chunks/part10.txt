>':''}</div>`}).join('');renderLineup()}
function renderCalendar(){let sch=G.schedules[G.division];$('calendarList').innerHTML=sch.map((games,i)=>{let g=games.find(x=>x.h===G.club||x.a===G.club);return g?`<div class=item>${i+1}ª rodada • <b>${g.h}</b> ${g.played?g.hg+' x '+g.ag:'x'} <b>${g.a}</b> ${i===G.round&&!g.played&&G.phase==='league'?'<span class=badge>PRÓXIMO</span>':''}</div>`:`<div class=item>${i+1}ª rodada • <span class=mut>Folga do ${G.club}</span></div>`}).join('')}
function renderLive(min=0){let rows=[];for(const d of ['A1','A2','B']){let po=G.phase==='playoffs'?G.playoffs?.[d]:null;rows.push(`<div class=phaseTitle>${DIV[d].name}${po?' • '+po.stage:G.phase==='league'?' • '+(G.round+1)+'ª rodada':''}</div>`);let games=currentGames(d);if(!games.length){rows.push(`<div class=item><span class=mut>${po?.winner?'Campeão: '+po.winner:'Sem jogos nesta data'}</span></div>`);continue}for(const g of games){let status=g.played?'FT':min?min+"'":'Pré-jogo';let extra=g.pen?` <span class=mut>(pên. ${g.pen})</span>`:'';rows.push(`<div class=liveGame><div class=teamSide><span class=clubDot style="background:${clubColor(g.h)}"></span><b>${g.h}</b></div><div class=liveScore>${g.hg} x ${g.ag}</div><div class="teamSide away"><b>${g.a}</b><span class=clubDot style="background:${clubColor(g.a)}"></span></div><span class=mut>${status}${extra}</span></div>`)}}$('allLive').innerHTML=rows.join('')}
function renderCup(){if(!G.cup){$('cupInfo').innerHTML='<p class=mut>A Copinha começa depois do fim do Gauchão. Clubes das três divisões participam.</p>';$('cupList').innerHTML='';return}if(G.cup.winner){$('cupInfo').innerHTML=`<p><b>Campeão:</b> ${G.cup.winner}<br><b>Supercopa:</b> ${G.cup.superWinner||'A definir'}</p>`}else $('cupInfo').innerHTML=`<p>${G.cup.teams.length} clubes restantes. <button class="btn green" onclick="playCup()">Jogar fase</button></p>`;$('cupList').innerHTML=G.cup.log.map(x=>`<div class=item>${x}</div>`).join('')}
function renderHistory(){$('historyList').inne